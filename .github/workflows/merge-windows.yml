name: Windows 

on:
  pull_request: 
    branches: [ "master" ]

jobs:
  windows-build-test:
    uses: ./.github/workflows/windows.yml
    with: 
      mx-rebuild: false
      build-tests: true
      build-type: Release
