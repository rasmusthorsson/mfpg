name: Windows

on:
  push:
    branches: [ "feature-45-dynamically-linked-actions" ]
  pull_request:
    branches: [ "master" ]

jobs:
  windows-build-test:
    uses: ./.github/workflows/wf-main.yml
    with: 
      os: windows-latest
      c-compiler: cl
      cxx-compiler: cl
      mx-rebuild: false
      build-tests: true
      build-type: Release
